# Future Works

The following files are not to be used right now. They are included in the project for potential future development and deployment:

- `src/api.py`: REST API for serving model predictions (FastAPI)
- `frontend.py`: Streamlit or React-based user interface for interacting with the API
- `fraud-frontend.py`

These components are not currently active or maintained. Please ignore them for now. 